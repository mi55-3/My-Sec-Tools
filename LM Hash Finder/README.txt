Make the lm-hash-finder.sh an executable

chmod +x lm-hash-finder.sh

and run the command as following 
./lm-hash-finder.sh

please make sure the ntds.txt file is located within the the same directory as the script or ensure you change the path. 

